#include "main.h"
/**
 * add - Adds two values
 * Description: to add two integers
 * @a: The first number to be computed
 * @b: The second number to be computed
 * Return: (0)
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
