#include <stdio.h>
/**
 * main - print the number of base 10
 *
 * Return: always (0)
 *
 */
int main(void)
{
	int base_ten;

	for (base_ten = 0; base_ten < 10; base_ten++)
	{
		printf("%d\n", base_ten);
	}
	return (0);
}
